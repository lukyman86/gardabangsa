import { createStart } from '@tanstack/react-start'

export const startInstance = createStart(() => ({
  // Default Start instance. Extend with requestMiddleware /
  // functionMiddleware / serverFns.fetch here if needed.
}))
